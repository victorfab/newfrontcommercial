import { FechaTarjetasPipe } from './fecha-movimientos.pipe';

describe('FechaTarjetasPipe', () => {
  it('create an instance', () => {
    const pipe = new FechaTarjetasPipe();
    expect(pipe).toBeTruthy();
  });
});
