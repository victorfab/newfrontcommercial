import { FechaMovPdfPipe } from './fecha-mov-pdf.pipe';

describe('FechaMovPdfPipe', () => {
  it('create an instance', () => {
    const pipe = new FechaMovPdfPipe();
    expect(pipe).toBeTruthy();
  });
});
