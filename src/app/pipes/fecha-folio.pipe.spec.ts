import { FechaFolioPipe } from './fecha-folio.pipe';

describe('FechaFolioPipe', () => {
  it('create an instance', () => {
    const pipe = new FechaFolioPipe();
    expect(pipe).toBeTruthy();
  });
});
