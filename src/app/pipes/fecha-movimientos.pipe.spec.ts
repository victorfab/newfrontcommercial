import { FechaMovimientosPipe } from './fecha-movimientos.pipe';

describe('FechaMovimientosPipe', () => {
  it('create an instance', () => {
    const pipe = new FechaMovimientosPipe();
    expect(pipe).toBeTruthy();
  });
});
