import { TransformWordPipe } from './transform-word.pipe';

describe('TransformWordPipe', () => {
  it('create an instance', () => {
    const pipe = new TransformWordPipe();
    expect(pipe).toBeTruthy();
  });
});
