import { TransformFechaPipe } from './transform-fecha.pipe';

describe('TransformFechaPipe', () => {
  it('create an instance', () => {
    const pipe = new TransformFechaPipe();
    expect(pipe).toBeTruthy();
  });
});
